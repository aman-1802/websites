import type { Metadata } from "next";import { OrderPage } from "../components";
export const metadata:Metadata={title:"Order Enquiry",description:"Review your gift request and send the details to Petal & Parcel on WhatsApp."};
export default function Page(){return <OrderPage/>}
