import type { Metadata } from "next";import { CorporatePage } from "../components";
export const metadata:Metadata={title:"Corporate Gifting",description:"Employee kits, client hampers, event gifts and bulk branded gifting in Ahmedabad."};
export default function Page(){return <CorporatePage/>}
